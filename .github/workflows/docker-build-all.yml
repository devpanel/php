name: Build all Docker images

on:
  workflow_dispatch:

jobs:
  detect-versions:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      matrix: ${{ steps.detect.outputs.matrix }}
      tag_suffix: ${{ steps.vars.outputs.tag_suffix }}
      latest_php_version: ${{ steps.detect.outputs.latest_php_version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Determine image tag suffix
        id: vars
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "tag_suffix=" >> $GITHUB_OUTPUT
          else
            echo "tag_suffix=-rc" >> $GITHUB_OUTPUT
          fi

      - name: Discover PHP version directories
        id: detect
        run: |
          # Build a JSON array of all version directories (names starting with a
          # digit), sorted semantically. Non-numeric directories are ignored.
          VERSIONS=$(find . -maxdepth 1 -mindepth 1 -type d -name '[0-9]*' \
            | sed 's|./||' | sort -V)
          MATRIX=$(printf '%s\n' $VERSIONS | jq -R . | jq -sc .)
          LATEST=$(printf '%s\n' $VERSIONS | tail -1)
          echo "matrix=${MATRIX}" >> $GITHUB_OUTPUT
          echo "latest_php_version=${LATEST}" >> $GITHUB_OUTPUT

  build:
    needs: detect-versions
    strategy:
      fail-fast: false
      matrix:
        version: ${{ fromJson(needs.detect-versions.outputs.matrix) }}
    permissions: {}
    uses: ./.github/workflows/reusable/build-php-version.yml
    with:
      php_version: ${{ matrix.version }}
      tag_suffix: ${{ needs.detect-versions.outputs.tag_suffix }}
      use_cache: false
      latest_php_version: ${{ needs.detect-versions.outputs.latest_php_version }}
    secrets: inherit

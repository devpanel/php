name: Docker build and push PHP7.4-advance

env:
  IMAGE_TAG: ${{ secrets.DOCKERHUB_USERNAME }}/php:7.4-advance-rc

on:
  push:
    paths:
      - "7.4/advance/**"
    paths-ignore:
      - "7.4/secure/**"
      - "7.4/base/**"
    branches:
      - main
      - develop
  workflow_run:
    workflows: ["Docker build and push PHP7.4-secure"]
    types:
      - completed
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.image-tag.outputs.image_tag }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - linux/amd64
          - linux/arm64
    steps:
      - name: Set env to production environment
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' || github.event_name == 'workflow_run' && github.event.workflow_run.head_branch == 'main' }}
        run: |
          echo "IMAGE_TAG=${{ secrets.DOCKERHUB_USERNAME }}/php:7.4-advance" >> $GITHUB_ENV

      - name: Export image tag output
        id: image-tag
        run: |
          echo "image_tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v6
        with:
          file: ./7.4/advance/Dockerfile
          context: ./7.4/advance/
          build-args: |
            # Allow overriding upstream base image namespace; defaults to devpanel in Dockerfile
            DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}
          tags: ${{ env.IMAGE_TAG }}
          platforms: ${{ matrix.platform }}
          cache-from: type=gha,scope=${{ github.workflow }}-${{ matrix.platform }}
          cache-to: type=gha,scope=${{ github.workflow }}-${{ matrix.platform }},mode=max
          outputs: type=image,push-by-digest=true,name-canonical=true,push=true

      - name: Export digest
        id: export-digest
        run: |
          mkdir -p ${{ runner.temp }}/digests
          digest="${{ steps.build.outputs.digest }}"
          platform="${{ matrix.platform }}"
          platform_safe="${platform//\//-}"
          echo "platform_safe=$platform_safe" >> $GITHUB_OUTPUT
          echo $digest > ${{ runner.temp }}/digests/$platform_safe.digest

      - name: Upload digest
        uses: actions/upload-artifact@v4
        with:
          name: digests-${{ steps.export-digest.outputs.platform_safe }}
          path: ${{ runner.temp }}/digests/
          if-no-files-found: error
          retention-days: 1

  merge:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Download digests
        uses: actions/download-artifact@v4
        with:
          path: ${{ runner.temp }}/digests
          pattern: digests-*
          merge-multiple: true

      - name: Create manifest list and push
        working-directory: ${{ runner.temp }}/digests
        run: |
          docker buildx imagetools create \
            -t ${{ needs.build.outputs.image_tag }} \
            $(for f in *.digest; do echo "${{ needs.build.outputs.image_tag }}@$(cat $f)"; done)

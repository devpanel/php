name: Compute effective before SHA
description: >
  Finds the effective per-platform "before" SHAs for a push or
  pull_request event, emitted as a JSON map (before_shas).

  For pull_request events, all platforms map to the PR base SHA.

  For push events, queries workflow runs for the specified workflow(s)
  and finds the closest reachable ancestor run's head SHA.  Only runs
  that included all requested platforms (verified via the
  platforms_filter annotation emitted by the detect-versions action)
  are considered.  Runs lacking the annotation are trusted
  unconditionally for backward compatibility.  When no qualifying prior
  run is found, before_shas is emitted as {} triggering a full rebuild.

  When `platforms` is empty, before_shas is emitted as {} (no
  filtering, caller should treat that as "build everything").

  Workflow discovery (push events only): fetches recent successful
  runs via workflow_files (bare filenames or numeric workflow IDs).

  Required GITHUB_TOKEN permissions on the calling job:
    actions:  read   (list workflow runs, jobs, and check-run annotations)
    contents: read   (compare commits via the Repos API)

inputs:
  workflow_files:
    description: >
      Space-separated list of bare workflow filenames (e.g. "test.yml")
      to query for successful runs.  Full github.workflow_ref values are
      not supported; pass only bare filenames or numeric workflow IDs.
    required: false
    default: ''
  current_sha:
    description: The current commit SHA (github.sha).
    required: true
  ref_name:
    description: The branch name (github.ref_name).
    required: true
  repository:
    description: The repository in owner/name format (github.repository).
    required: true
  github_token:
    description: Token used to call the GitHub API (github.token).
    required: true
  event_name:
    description: The triggering event name (github.event_name).
    required: true
  pr_base_sha:
    description: >
      The PR base commit SHA (github.event.pull_request.base.sha).
      Required when event_name is "pull_request".
    required: false
    default: ''
  platforms:
    description: >
      Space-separated platform keys to populate in the output map
      (e.g. "linux-amd64 linux-arm64").  When set, all keys in the
      output map point to the same closest-ancestor SHA.  When empty,
      before_shas is {} (no filtering, full rebuild).
    required: false
    default: ''

outputs:
  before_shas:
    description: >
      JSON map of platform-key to the closest reachable ancestor SHA,
      e.g. {"linux-amd64":"abc123","linux-arm64":"def456"}.
      All entries share the same SHA (the closest ancestor run).
      Emitted as {} when platforms is empty or no candidates are found.
    value: ${{ steps.compute.outputs.before_shas }}

runs:
  using: composite
  steps:
    - name: Compute effective before_sha
      id: compute
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.github_token }}
        WORKFLOW_FILES: ${{ inputs.workflow_files }}
        CURRENT_SHA: ${{ inputs.current_sha }}
        REF_NAME: ${{ inputs.ref_name }}
        REPOSITORY: ${{ inputs.repository }}
        EVENT_NAME: ${{ inputs.event_name }}
        PR_BASE_SHA: ${{ inputs.pr_base_sha }}
        PLATFORMS: ${{ inputs.platforms }}
      run: |
        set -euo pipefail

        if [[ "${EVENT_NAME}" == "pull_request" ]]; then
          if [[ -z "${PR_BASE_SHA}" ]]; then
            echo "::error::Input 'pr_base_sha' is required when event_name is 'pull_request'."
            exit 1
          fi
          # For pull_request events, all platforms share the same base SHA.
          # Build a before_shas map with each platform key pointing to it.
          if [[ -n "${PLATFORMS:-}" ]]; then
            read -r -a _pr_pkeys <<< "${PLATFORMS}"
            _pr_shas_json="{}"
            for _k in "${_pr_pkeys[@]}"; do
              _pr_shas_json="$(printf '%s' "${_pr_shas_json}" \
                | jq -c --arg k "${_k}" --arg v "${PR_BASE_SHA}" '. + {($k): $v}')"
            done
            echo "before_shas=${_pr_shas_json}" >> "$GITHUB_OUTPUT"
          else
            echo "before_shas={}" >> "$GITHUB_OUTPUT"
          fi
          exit 0
        fi

        if [[ -z "${PLATFORMS:-}" ]]; then
          echo "before_shas={}" >> "$GITHUB_OUTPUT"
          exit 0
        fi

        # For push events, collect (run_id, head_sha) pairs from prior successful
        # runs.  We record the run_id so we can later check which platforms each
        # run targeted (via the platforms_filter annotation emitted by the
        # detect-versions action), ensuring we only anchor against runs that
        # included all the platforms we are building now.
        declare -A SHA_TO_RUN_ID   # head_sha → run_id (last seen per SHA)

        if [[ -n "${WORKFLOW_FILES:-}" ]]; then
          NORMALIZED_WORKFLOW_FILES=$(printf '%s' "${WORKFLOW_FILES}" | tr '\r\n' '  ')
          read -r -a WF_ARRAY <<< "${NORMALIZED_WORKFLOW_FILES}"
          for wf in "${WF_ARRAY[@]}"; do
            while IFS=$'\t' read -r _rsha _rid; do
              [[ -n "${_rsha}" && -n "${_rid}" ]] \
                && SHA_TO_RUN_ID["${_rsha}"]="${_rid}"
            done < <(gh api \
              "repos/${REPOSITORY}/actions/workflows/${wf}/runs?branch=${REF_NAME}&status=success&per_page=10" \
              --jq '.workflow_runs[] | [.head_sha // "", (.id | tostring)] | join("\t")' \
              || true)
          done
        fi

        # Deduplicate candidate SHAs; discard malformed entries.
        readarray -t CANDIDATE_SHAS < <(
          printf '%s\n' "${!SHA_TO_RUN_ID[@]}" \
            | grep -E '^[0-9a-f]{40}$' \
            | sort -u \
            || true
        )

        # Cache for SHA → ahead_by or "unreachable".
        declare -A SHA_AHEAD_CACHE

        # compare_sha <sha>
        # Sets COMPARE_AHEAD to the numeric ahead_by value, or "" when the
        # SHA is not a reachable ancestor of CURRENT_SHA.
        compare_sha() {
          local _sha="$1"
          if [[ -v "SHA_AHEAD_CACHE[${_sha}]" ]]; then
            COMPARE_AHEAD="${SHA_AHEAD_CACHE[${_sha}]}"
            [[ "${COMPARE_AHEAD}" == "unreachable" ]] && COMPARE_AHEAD=""
            return
          fi
          local _compare _status _ahead
          _compare=$(gh api \
            "repos/${REPOSITORY}/compare/${_sha}...${CURRENT_SHA}" \
            --jq '{status: .status, ahead_by: .ahead_by}' 2>/dev/null || true)
          _status=$(printf '%s' "${_compare}" | jq -r '.status // ""')
          _ahead=$(printf '%s' "${_compare}" | jq -r '.ahead_by // empty')
          if [[ "${_status}" == "ahead" || "${_status}" == "identical" ]] \
             && [[ "${_ahead}" =~ ^[0-9]+$ ]]; then
            SHA_AHEAD_CACHE["${_sha}"]="${_ahead}"
            COMPARE_AHEAD="${_ahead}"
          else
            SHA_AHEAD_CACHE["${_sha}"]="unreachable"
            COMPARE_AHEAD=""
          fi
        }

        # Cache for run_id → platforms_used (comma-separated platform paths,
        # e.g. "linux/amd64,linux/arm64"), or "" when the annotation is absent.
        declare -A RUN_PLATFORMS_CACHE

        # get_run_detect_platforms <run_id>
        # Sets RUN_DETECT_PLATFORMS to the comma-separated platform list from
        # the platforms_filter annotation emitted by the detect job in that run,
        # or "" when the annotation is absent (old run predating the annotation).
        # On absence the caller should trust the run (backward compatibility).
        get_run_detect_platforms() {
          local _run_id="$1"
          if [[ -v "RUN_PLATFORMS_CACHE[${_run_id}]" ]]; then
            RUN_DETECT_PLATFORMS="${RUN_PLATFORMS_CACHE[${_run_id}]}"
            return
          fi
          # Find the detect/Detect job for this run.
          local _job_id
          _job_id="$(gh api \
            "repos/${REPOSITORY}/actions/runs/${_run_id}/jobs?filter=all" \
            --jq '[.jobs[] | select(.name | ascii_downcase | contains("detect")) | .id][0] // ""' \
            2>/dev/null || echo "")"
          if [[ -z "${_job_id}" ]]; then
            RUN_PLATFORMS_CACHE["${_run_id}"]=""
            RUN_DETECT_PLATFORMS=""
            return
          fi
          local _plats
          _plats="$(gh api \
            "repos/${REPOSITORY}/check-runs/${_job_id}/annotations" \
            --jq '[.[] | select(.title == "platforms_filter") | .message][0] // ""' \
            2>/dev/null || echo "")"
          RUN_PLATFORMS_CACHE["${_run_id}"]="${_plats}"
          RUN_DETECT_PLATFORMS="${_plats}"
        }

        # run_covers_all_platforms <platforms_csv> <platform_keys_space_sep>
        # Returns 0 when every platform key in the space-separated list maps
        # to a platform path present in the comma-separated CSV.
        run_covers_all_platforms() {
          local _csv="$1" _pkeys="$2"
          local _pkey _plat
          for _pkey in ${_pkeys}; do
            _plat="${_pkey//-/\/}"
            if ! printf '%s' "${_csv}" | tr ',' '\n' | grep -qxF "${_plat}"; then
              return 1
            fi
          done
          return 0
        }

        read -r -a PLATFORM_KEYS <<< "${PLATFORMS}"

        # Find the closest reachable ancestor run that included ALL requested
        # platforms.  Runs that lack the platforms_filter annotation (old runs
        # predating the annotation) are trusted unconditionally for backward
        # compatibility.
        BEST_SHA=""
        BEST_AHEAD=999999999

        for sha in "${CANDIDATE_SHAS[@]+"${CANDIDATE_SHAS[@]}"}"; do
          [[ -z "${sha}" || "${sha}" == "${CURRENT_SHA}" ]] && continue
          compare_sha "${sha}"
          [[ -z "${COMPARE_AHEAD}" ]] && continue
          if [[ "${COMPARE_AHEAD}" -lt "${BEST_AHEAD}" ]]; then
            # Check whether this run targeted all our platforms.
            local_run_id="${SHA_TO_RUN_ID[${sha}]:-}"
            if [[ -n "${local_run_id}" && -n "${PLATFORMS}" ]]; then
              get_run_detect_platforms "${local_run_id}"
              if [[ -n "${RUN_DETECT_PLATFORMS}" ]]; then
                run_covers_all_platforms "${RUN_DETECT_PLATFORMS}" "${PLATFORMS}" \
                  || continue
              fi
            fi
            BEST_SHA="${sha}"
            BEST_AHEAD="${COMPARE_AHEAD}"
          fi
        done

        # Build before_shas: all platforms share the closest ancestor SHA.
        BEFORE_SHAS_JSON="{}"
        if [[ -n "${BEST_SHA}" ]]; then
          for pkey in "${PLATFORM_KEYS[@]}"; do
            BEFORE_SHAS_JSON="$(printf '%s' "${BEFORE_SHAS_JSON}" \
              | jq -c --arg k "${pkey}" --arg v "${BEST_SHA}" '. + {($k): $v}')"
          done
        fi
        echo "before_shas=${BEFORE_SHAS_JSON}" >> "$GITHUB_OUTPUT"
